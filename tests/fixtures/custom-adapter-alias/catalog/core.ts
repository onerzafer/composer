// Shared helper the catalog imports via this adapter's own `@/*` alias
// (declared in this package's tsconfig.json as `"@/*": ["catalog/*"]`)
// instead of a plain relative `./core` import.
export const CORE_LABEL = "Note";
export const CORE_VERSION = "1.0.0";
