// Minimal output map — only `discover()` (not `compose`) exercises this
// fixture, and `discover()` only checks that this file exists; it never
// invokes the map itself. See tests/integration/sifir-catalog-zod-v4.test.ts.
const outputMap = { byPrimitive: {} };
export default outputMap;
